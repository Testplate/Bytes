# Bytes
Bytes: A clean, organized tool to extract raw hex bytes from any file (GIF, PDF, EXE, etc.) into C++ arrays. Includes a run.bat launcher with auto-dependency setup. Simple, efficient, and ready for C++ integration.
